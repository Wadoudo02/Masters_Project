{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File (Root Directory)",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}